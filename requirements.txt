# Core (always installed)
rich==13.7.1
pytest==8.2.0

# AWS (install if scanning AWS)
boto3==1.34.84
botocore==1.34.84

# Azure (install if scanning Azure)
azure-identity==1.16.0
azure-mgmt-compute==32.0.0
azure-mgmt-network==25.3.0
azure-mgmt-resource==23.0.1

# GCP (install if scanning GCP)
google-cloud-compute==1.18.1
